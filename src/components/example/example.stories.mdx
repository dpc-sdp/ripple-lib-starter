import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import Example from './example.vue'

export const SingleTemplate = (args) => ({
  components: { Example },
  setup() {
    return { args }
  },
  template: '<Example v-bind="args" />'
})

<Meta title='Components/Example' component={Example} />

# Example

<ArgsTable of={Example} />

## Default

<Canvas>
  <Story
    name='Default'
    args={{
      msg: 'The count is :'
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>